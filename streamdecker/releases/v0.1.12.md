## Streamdecker v0.1.12

### New Features
- Launch button with folder picker support:
  - Short press: Opens fzf folder picker to select project
  - Long press (500ms+): Launches kiro-cli in current directory

### Fixes
- Fixed AppleScript to use `command` parameter instead of `write text`
- Uses `/bin/zsh -lic` for proper login shell environment
